# Create a program that converts meters to centimeters

meters = float(input("How many meters would you like to convert to centimeters? "))

calc = meters * 100

print("{} meters in centimeters is {:.2f}.".format(meters, calc))